version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
    commands:
      - echo Passou pela fase de instalação

  build:
    commands:
      - gradle clean build
      - gradle test

  post_build:
    commands:
      - gradle dockerfile
      - gradle dockerBuild
      - echo Build completed on `date`

artifacts:
  files:
    - build/libs/key-manager-rest-*-all.jar